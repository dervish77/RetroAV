RetroAV src
===========

Notes:

- RetroAV is the default Arduino source code for the RetroAV shield which turns the system into a simple text terminal that could be hooked up to a host computer (i.e. retro computer, raspberry pi, etc).

